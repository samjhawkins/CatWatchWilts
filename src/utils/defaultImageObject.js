import imageNotFound from '../images/image-not-found.svg';

const defaultImageObject = {
  imageId: '0',
  image: imageNotFound,
  imageName: 'Image could not be found',
};

export default defaultImageObject;
